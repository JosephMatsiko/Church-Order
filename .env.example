# Optional. Without it, the Ask box never appears.
ANTHROPIC_API_KEY=
ANTHROPIC_MODEL=claude-sonnet-5

# Optional. Overrides the canonical origin used for Open Graph and Twitter cards.
# On Vercel this is derived from VERCEL_PROJECT_PRODUCTION_URL automatically.
SITE_URL=
